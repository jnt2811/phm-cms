const admin = "/admin";
const volunteer = "/volunteer";
const clinic = "/clinic";

const pathNames = {
  MAIN: "/",

  // Auth
  LOGIN: "/login",

  // Admin
  ADMIN: admin,

  // Volunteer
  VOLUNTEER: volunteer,

  // Clinic
  CLINIC: clinic,
};

export default pathNames;
